{
  "name": "resume-ai-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "sh -c 'cd resume-ai && npm install && npm run build'",
    "dev": "sh -c 'cd resume-ai && npm run dev'",
    "start": "node server/src/index.js"
  }
}
