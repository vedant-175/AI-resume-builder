{
  "name": "resume-ai-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install": "npm --prefix server install && npm --prefix resume-ai install",
    "build": "npm run install && npm --prefix resume-ai run build",
    "dev": "sh -c 'cd resume-ai && npm run dev'",
    "start": "npm --prefix server start"
  }
}
